{
    "version": 2,
    "outputDirectory": "build/web",
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "build/web"
            }
        }
    ]
}